#include <bits/stdc++.h>

#define int long long

using namespace std;

signed main() {
    int h;
    cin >> h;
    float ans = sqrt((12800000 * h) * h);
    cout << fixed << setprecision(6) << ans << endl;
    return 0;
}