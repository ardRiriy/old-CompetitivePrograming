#include <bits/stdc++.h>

using namespace std;

#define PRINT(x) cout << x << endl;

int main() {
    int n;
    cin >> n;

    if (n == 12)
        PRINT(1)
    else
        PRINT(n + 1)

    return 0;
}