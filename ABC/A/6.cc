#include <bits/stdc++.h>

using namespace std;

#define PRINT(x) cout << x << endl;

int main() {
    int x;
    cin >> x;
    if (x % 3 == 0) {
        PRINT("YES");
    } else
        PRINT("NO");

    return 0;
}